
<h1>Listing posts</h1>

<table>
  <tr>
  	<th>#</th>
    <th>Elo</th>
    <th>Team name</th>
  </tr>

<% @posts.each_with_index do |post, index| %>
  <tr>
    <td><%= index+1 %></td>
    <td><%= post.team_elo %></td>
    <td><%= post.team_name %></td>

  </tr>
<% end %>
</table>

<br />

<%#(replace with % to use) link_to 'New Post', new_post_path %>

<%#(replace with % to use) link_to 'Refresh', {:controller=>"posts", :action=>"create_new_list"}%>